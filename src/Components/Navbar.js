import '../Styles/Navbar.css'

function Navbar(props){
    return(
        <div>
            <nav>
                <h1>Simplest Working React Calculator</h1>
            </nav>
        </div>
    )
}

export default Navbar;